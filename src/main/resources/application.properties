
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${WALLET_DATABASE_URL}
spring.datasource.username=${WALLET_DATABASE_USERNAME}
spring.datasource.password=${WALLET_DATABASE_PASSWORD}
spring.datasource.driver-class-name =com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLInnoDBDialect

# jwt
wallet.jwt.secret=${WALLET_JWT_SECRET}
wallet.jwt.expiration=86400000